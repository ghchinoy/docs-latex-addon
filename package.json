{
  "name": "google-docs-latex",
  "version": "1.2.0",
  "description": "Render LaTeX equations into native Google Docs symbols and script formatting without external services or images.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ghchinoy/docs-latex-addon.git"
  },
  "main": "Code.js",
  "scripts": {
    "test": "node tests/verify.js",
    "bind": "sh -c 'rm -f .clasp.json && clasp create --parentId \"$1\" --title \"LaTeX Math Extension\" && git checkout appsscript.json && clasp push -f' --",
    "push": "clasp push -f",
    "pull": "clasp pull",
    "watch": "clasp push --watch"
  },
  "keywords": [
    "google-docs",
    "latex",
    "math",
    "apps-script",
    "google-workspace",
    "unicode",
    "equations"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
